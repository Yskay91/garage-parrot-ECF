{% extends 'base.html.twig' %}

{% block title %}Gge Parrot - Inscription
{% endblock %}

{% block body %}
	<div class="text-center">
		<div class="container-fluid py-5">
			<h1 class="titrePages display-5 fw-bold">Inscription</h1>
			<p class="titrePages fs-4"></p>
		</div>
	</div>

        {{form(form)}}
	{# <form action="{{ path('security.registration') }}" method="post" class="col-md-5">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}
		<div class="form-group mt-4">
			<label for="email">Email</label>
			<input type="email" value="{{ last_username }}" name="_email" id="email" class="form-control" autocomplete="email" required>
		</div>
		<div class="form-group mt-4">
			<label for="password">Mot de passe</label>
			<input type="password" name="_password" id="password" class="form-control" required>
		</div>

		<button class="btn btn-primary mt-4" type="submit">
			Connexion
		</button>
	</form> #}

{% endblock %}
